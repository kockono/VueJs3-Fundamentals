// Tenemos que especificar en donde queremos renderizar nuestra apliación
const app = Vue.createApp({
    // template: `
    // <h1> Hola Mundo</h1>
    // <p>Desde app.js</p>
    // `
    
})

console.log(`Vue`, Vue)
app.mount('#myApp')